# 한민고등학교 학생 선택 웹앱 배포

이 폴더는 데스크톱 앱의 현재 설정으로 만든 학생 선택 사이트(`site/`)와 Google Apps Script 설정 키트(`setup-kit/`)입니다. **올리는 것은 `site/` 폴더뿐**이고, `setup-kit/`는 시트를 준비할 때만 쓰며 공개하지 않습니다. 현재 로그인 방식은 **Google 학교 계정**입니다. 학생 명부·비밀번호·기존 제출 데이터는 들어 있지 않습니다.

## 1. Google 스프레드시트와 Apps Script 준비

1. `setup-kit/새-선택조사-시트-원본.xlsx`를 Google Drive에 올리고 **Google 스프레드시트로 변환**합니다. 기존 운영 시트에 가져오거나 덮어쓰지 않습니다.
2. `확장 프로그램 → Apps Script`를 엽니다.
3. `setup-kit/apps-script/Code.gs`의 내용을 기본 `Code.gs`에 전부 붙여 넣고 저장합니다.
4. 함수 목록에서 `setupSelectionApp`을 선택해 한 번 실행하고 권한을 승인합니다.
5. 스프레드시트의 `_config` 시트에서 최종 조사 차수 **3차**와 시작·종료 시각을 입력합니다. 설치 직후에는 실수로 접수가 열리지 않도록 모든 기간이 비어 있습니다.
6. 데스크톱 앱의 `설정 키트 만들기`로 생성했다면 `학생명단-데스크톱연동.csv`를 `_students`에 가져옵니다. 학번·이메일·이름·학년·입학연도가 데스크톱 재학생 명단에서 이미 채워져 있습니다. 이 CSV는 비공개 운영자료이므로 GitHub 저장소에 올리지 않습니다. 범용 키트만 받은 경우에는 아래 형식대로 직접 붙여 넣습니다. 첫 행 제목은 바꾸지 않습니다.
   - `student_id`: 학번(앞의 0이 사라지지 않도록 텍스트)
   - `email`: Google 방식은 학교 이메일 필수, 비밀번호 방식은 비워도 됨
   - `name`, `grade`, `entry_year`: 이름·현재 학년·4자리 입학연도
   - `initial_password`: 비밀번호 방식에서만 입력
   - `active`: 재학생은 `TRUE`
   - `login_id`: 교사 지정 아이디 방식에서만 학생별 로그인 아이디 입력. 학번 방식에서는 비워도 됨
   - `class_no`, `number`: 현재 반·번호(선택). 담임 현황 페이지의 반별 집계에 씁니다. 비우면 5자리 학번에서 추정합니다
7. Google 방식으로 교사 테스트를 허용할 계정은 `_teachers`에 이메일·이름·`TRUE`를 입력합니다.
8. 3차 조사에서 2차 제출을 불러와야 하면 `_students` 명단을 먼저 입력한 뒤 `_config.PREVIOUS_SPREADSHEET_ID`에 기존 운영 시트 ID를 입력하고, 메뉴의 `이전 차수 제출만 가져오기`를 실행합니다. 기존 시트는 읽기만 하며 계정·세션·다른 차수 자료는 복사하지 않습니다.
9. Apps Script에서 `배포 → 새 배포 → 웹 앱`을 선택합니다.
   - 실행 사용자: 나
   - 액세스 권한: 모든 사용자
10. 배포 뒤 받은 `https://script.google.com/macros/s/.../exec` 주소를 데스크톱 앱의 `응답 저장 연결`에 붙여 넣습니다.
11. 주소 뒤에 `?schedule=1`을 붙여 열었을 때 `"ok":true`, `"finalRound":3`가 보이면 연결 준비가 된 것입니다.

현재 설정에 저장된 Apps Script 주소: `https://script.google.com/macros/s/AKfycbx8r66SmJkTOjgxF-n_LTgd833ZQhqQW1hynjpDg6xsfDWgf9mM6x5u_3fCkEn3HL6f/exec`

## 2-A. Google 학교 계정 방식

1. Google Cloud Console에서 웹 애플리케이션용 OAuth 클라이언트를 만듭니다.
2. 승인된 JavaScript 원본에 `https://<GitHub아이디>.github.io`를 추가합니다. 저장소 이름 같은 경로는 넣지 않습니다.
3. 발급된 `...apps.googleusercontent.com` 값을 데스크톱 앱의 `Google 로그인 연결`에 붙여 넣습니다.
4. Apps Script를 먼저 설치했다면 스프레드시트 `_config`의 `GOOGLE_CLIENT_ID`에도 같은 값을 붙여 넣고 Apps Script 웹 앱을 새 버전으로 다시 배포합니다.
5. Google 로그인 이메일이 `_students.email`과 일치해야 학생으로 로그인됩니다. `_teachers`에 등록된 이메일만 교사 테스트 모드로 들어갑니다.

허용 학교 도메인: `hanmin.hs.kr`
현재 설정에 저장된 OAuth Client ID: `817402337132-buq4v80hslbv80d2ajteaj8h5664hod2.apps.googleusercontent.com`

## 2-B. 학교 이메일이 없는 경우: 아이디·비밀번호 방식

현재 아이디 정책은 **명부의 학번 그대로**입니다.

1. 교사 지정 아이디 방식이면 `_students.login_id`에 학교가 정한 학생별 아이디를 입력합니다. 영문·숫자·한글과 `._@-`를 쓸 수 있고, 공백 없이 1~64자여야 하며 대소문자 구분 없이 중복되면 안 됩니다. 학번 방식이면 비워 둡니다.
2. `_students.initial_password`에 학생별 임시 비밀번호를 입력합니다. 학번·로그인 아이디와 같은 값처럼 쉽게 추측되는 값은 피하세요.
3. 스프레드시트를 새로고침한 뒤 `선택과목 웹앱 → 학생 명단의 계정 만들기`를 실행합니다.
4. 스크립트가 아이디 중복을 검사하고 학생별 salt와 스크립트 전용 비밀키를 사용한 HMAC-SHA-256 값만 `_accounts`에 저장한 뒤, `_students.initial_password` 원문을 즉시 지웁니다.
5. 학생은 첫 로그인 때 새 비밀번호로 변경합니다. 최소 길이는 **8자**입니다.
6. 로그인 아이디만 바꾸려면 `_students.login_id`를 수정하고 같은 메뉴를 다시 실행합니다. 새 임시 비밀번호는 필요 없습니다.
7. 학생을 추가하거나 비밀번호를 초기화할 때만 해당 행에 새 임시 비밀번호를 넣고 같은 메뉴를 다시 실행합니다. 기존 제출 기록은 지워지지 않습니다.
8. 로그인 실패는 아이디별로 일정 횟수 이상 반복되면 잠시 차단됩니다. `_config`의 `LOGIN_MAX_FAILURES`, `LOGIN_WINDOW_MINUTES`, `LOGIN_BLOCK_MINUTES`에서 학교 정책에 맞게 조정할 수 있습니다.

Google Client ID는 이 방식에서 필요하지 않습니다. 학생 명단과 비밀번호 원문을 GitHub 저장소에 올리지 마세요.

## 3. 사이트 올리기 — 어디든 됩니다

`site/` 폴더는 정적 파일 묶음이라 GitHub Pages, Netlify, Vercel, 학교 웹서버 어디에나 그대로 올릴 수 있습니다. 어디에 올리든 규칙은 두 가지뿐입니다.

- 주소가 **https** 여야 합니다. Google 로그인은 http 주소에서 동작하지 않습니다.
- Google 로그인 방식이면, 올린 주소(예: `https://학교.netlify.app`, `https://계정.github.io`)를 Google Cloud OAuth 클라이언트의 **승인된 JavaScript 원본**에 등록해야 합니다. 저장소 이름 같은 경로는 넣지 않습니다.

올린 뒤에는 데스크톱 앱의 `배포 확인`에 주소를 넣어 마지막으로 만든 판이 올라갔는지, 로그인·응답 주소가 맞는지 대조하세요.

### GitHub Pages

`site/` 안에 `.github/workflows/deploy-pages.yml`이 들어 있습니다.

1. GitHub에 비어 있는 공개 저장소를 하나 만듭니다.
2. `site/` 폴더의 **내용**을 저장소 `main` 브랜치에 올립니다(`site` 폴더 자체가 아니라 그 안의 파일들이 저장소 루트에 오게).
3. 저장소 `Settings → Pages → Source`에서 `GitHub Actions`를 선택합니다.
4. `Actions`의 `학생 선택 웹앱 배포`가 끝나면 표시된 Pages 주소를 엽니다.

### Netlify

1. Netlify에 로그인해 `Add new site → Deploy manually`를 엽니다.
2. `site/` 폴더를 그대로 끌어다 놓습니다. 빌드 설정은 필요 없습니다(`netlify.toml`이 들어 있습니다).
3. 다시 올릴 때는 그 사이트의 `Deploys`에 새 `site/` 폴더를 끌어다 놓으면 됩니다. 이전 판으로 되돌리기도 그 화면에서 합니다.

### Vercel

1. Vercel에서 새 프로젝트를 만들고 `site/` 폴더를 올립니다(`vercel.json`이 들어 있어 별도 설정이 없습니다).
2. 또는 GitHub 저장소에 `site/` 내용을 올려 두고 Vercel에 그 저장소를 연결합니다.

### 학교 웹서버

`site/` 폴더의 내용을 https 로 서비스되는 폴더에 복사합니다. `.json` 파일이 `application/json`으로, 폴더 주소가 그 안의 `index.html`로 열리는지만 확인하면 됩니다.

### 올린 뒤 확인

첫 화면, 로그인, 학생 제출, 스프레드시트 기록, 로그아웃 후 재로그인했을 때 최종 선택 복원, 같은 계정 재제출 덮어쓰기를 차례로 확인합니다.

조사 공지는 데스크톱 앱의 `조사 공지`에 제목과 글을 쓰면 학생 첫 화면에 그대로 보입니다. 학교가 만든 공지 HTML 파일을 대신 지정하면 그 파일이 `notice.html`로 실려 글 대신 보입니다.

교육과정 설명회 슬라이드는 학교가 만든 HTML 파일 하나를 데스크톱 앱의 `교육과정 안내 슬라이드`에 지정하면 사이트에 `slides.html`로 실리고, 첫 화면과 상단 메뉴에 `교육과정 안내` 버튼이 생깁니다. 지정하지 않으면 버튼 자체가 보이지 않습니다.

이전 조사의 과목별 선택 인원을 카드에 보여 주려면 학번 폴더(`app/cohorts/<입학연도>/`)에 비어 있는 `counts-snapshot.json`을 같은 이름의 채운 파일로 바꿔 넣으면 됩니다. 형식은 `{"grade1": {"counts": {"<과목ID>": 인원}}, "grade2": {"counts": {...}}}`이며, 비어 있으면 표시하지 않습니다.

## 4. 제출 현황 보기 — 담당자와 담임

- **담당자(데스크톱 앱)**: «선택웹 팩 → 내보내기 → 제출 현황»에서 «시트에서 읽기»를 누르면 차수별로 학년·반별 제출 인원, 미제출자, 과목별 선택 인원이 보입니다. 시트의 `_config.DESKTOP_TOKEN`이 이 데스크톱의 관리자 코드와 같아야 하며, 설정 키트로 만든 시트에는 이미 들어 있습니다.
- **담임(웹)**: 사이트 주소 뒤에 `/teacher/`를 붙여 엽니다(예: `https://학교.netlify.app/teacher/`). Google 방식이면 `_teachers`에 등록된 학교 계정으로 로그인하고, 그 외에는 담당자가 알려 준 관리자 코드를 넣습니다. 자기 반을 누르면 미제출 학생이 보이고 «미제출자 목록 복사»로 명단을 받을 수 있습니다.
- 관리자 코드는 제출 현황을 읽는 열쇠입니다. 학생에게 알려 주지 마세요. 바꾸려면 시트 `_config.DESKTOP_TOKEN`을 새 값으로 고치고, 데스크톱 앱에서 설정 키트를 다시 만들면 같은 값이 나옵니다.

## 5. 운영 전 최소 점검

- 데스크톱 앱의 `운영본 사전 검사`가 통과했는지
- 각 입학연도 팩의 과목·선택군·학점·조사 차수가 맞는지
- `_config`의 현재 시각이 접수 기간 안인지
- `_config.PREVIOUS_SPREADSHEET_ID`가 새 시트 자신의 ID가 아니며, 가져오기 뒤 기존 시트의 행 수·수정 시각이 변하지 않았는지
- 선생님 테스트 제출이 `is_test=true`로 기록되는지
- 학생 계정 재제출 시 새 행이 늘지 않고 기존 행이 갱신되는지
- 다른 기기에서 같은 학생으로 로그인해도 같은 차수의 최종 제출이 바로 보이는지
- Google 방식은 `_students.email`, 비밀번호 방식은 설정에 따라 `_students.student_id` 또는 `_students.login_id`로 로그인되는지

학생 선택은 로그인 아이디가 아니라 변경되지 않는 `_students.student_id` 기반 계정 고유키로, 대상 학년별·조사 차수별 보관됩니다. 로그인 아이디·비밀번호를 바꾸거나 GitHub Pages 사이트를 다시 올려도 이 스프레드시트를 그대로 사용하는 한 기존 선택은 유지됩니다.

`이전 차수 제출만 가져오기`도 이 원칙에 맞춰 복사된 행의 `identity_key`를 `student:<student_id>`로 정규화합니다. 전체 백업 복원이나 기존 시트 덮어쓰기는 사용하지 않습니다.

설정이 바뀌면 데스크톱 앱에서 사이트 폴더를 다시 만든 뒤 새 `site/`를 같은 곳에 다시 올리면 됩니다.
